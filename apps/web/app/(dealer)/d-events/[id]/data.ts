// Re-export getDealerEventDetail from the parent events/data.ts so the detail
// page can import from "./data" (consistent with other feature directories).
export { getDealerEventDetail } from "../data";
